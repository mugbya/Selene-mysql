# 计划：调整表格列筛选按钮的默认样式

## 任务概述
调整表格列上筛选按钮的默认样式，使其默认情况下与列名背景色相同（不那么显眼）。

## 当前状态分析
- **文件位置**: `/Users/mugbya/git-files/Selene-mysql/Selene-mysql/src/components/common/table/LazyLoadDataTable.tsx`
- **行号**: 第 297 行和第 303 行
- **当前样式**:
  - 默认情况下按钮颜色为 `text-gray-300`（浅灰色）
  - 有筛选激活时为 `text-green-600`（绿色）

## 修改方案
1. 将筛选按钮的默认颜色从 `text-gray-300` 改为 `text-gray-700` 或 `text-gray-500`
2. 或者使用透明色/与背景同色，使按钮在默认状态下不那么明显

## 具体修改步骤
1. 修改第 297 行的按钮 className:
   - 将 `text-gray-300` 改为 `text-gray-500`（与列名文字颜色 `text-gray-700` 更协调）
   
2. 可选：根据需要调整第 303 行 Filter 图标的默认样式

## 预期效果
- 筛选按钮默认情况下颜色变暗，与列名文字颜色接近
- 当有筛选激活时仍然显示绿色 (`text-green-600`)，保持明显的视觉提示
